[gcode_macro CARTO_OFFSET_RESULT]
gcode:
    {% set new_offset_z = "%.3f"|format(printer.cartographer.touch.last_z_result|float * -1) %}
    RESPOND MSG="Carto offset: {new_offset_z}"
    
[gcode_macro PRINT_START]		#PRINT_START
gcode:
  {% set TOOL = params.TOOL | default(0)| int %}
  {% set BED_TEMP = params.BED|int %}
  {% set TOOL_TEMP = params.EXTRUDER|int %}
  {% set CHAMBER_TEMP = params.CHAMBER|default("45")|int %}
  {% set FILAMENT_TYPE = params.FilamentType|default("ABS")|string %}
  
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  START_TOOL_CRASH_DETECTION
  
  M117 Initializing
  INITIALIZE_TOOLCHANGER
  {% if "xyz" not in printer.toolhead.homed_axes %}
    M117 Homing 
    G28
    M117 Quad Gantry Level
    QUAD_GANTRY_LEVEL
    M117 Homing 
    G28
    M117 Quad Gantry Level
    QUAD_GANTRY_LEVEL
    M117 Homing Z Only
    G28 Z
  {% endif %}
 
  T0
  
  M117 Quad Gantry Level
  QUAD_GANTRY_LEVEL
  M117 Homing Z ONLY
  G28 Z
  
  BED_MESH_CLEAR
  {% if FILAMENT_TYPE in ["ABS", "TPU"] %}
    SKEW_PROFILE LOAD={FILAMENT_TYPE}
  {% endif %}

  M117 Heating Bed
  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}
    M106 S255                                           # Turn on the PT-fan
    SET_FAN_SPEED FAN=nevermore SPEED=1.0
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{BED_TEMP}                                    # Set the target temp for the bed    
    M117 Heating chamber
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={CHAMBER_TEMP}   # Waits for chamber temp
 
  # If the bed temp is not over 90c, then skip the chamber heating and just heat up to set temp with a 5 min soak
  {% else %}
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{BED_TEMP}                                    # Set the target temp for the bed
    G4 P300000                                          # Wait 5 min for the bedtemp to stabilize
  {% endif %}

  # Heat hotend to 150c. This helps with getting a correct Z-home.
  M109 S150                                            # Heat hotend to 150c

  M117 Cleaning Nozzle
  CLEAN_NOZZLE
  
#  M117 Tapping Z-Offset
# CARTOGRAPHER_TOUCH_HOME 

  M117 Bed Mesh
  BED_MESH_CALIBRATE

  M117 Selecting Correct Tool
  G1 X{x_wait} Y{y_wait} Z15 F9000  
  {% if TOOL > 0 %}
    M104 T0 S0 ;
    T{params.TOOL}
  {% endif %}

  M117 Waiting on T{params.TOOL} S{TOOL_TEMP}C
  M109 S{TOOL_TEMP} ; Wait for the tool to reach the target temperature
  
  G0 Z2 F300 ;Move up a bit
  G92 E0 ; Zero extruder
    
  # Get ready to print by doing a primeline and updating the LEDs
  M117 Printing
  G0 X{x_wait - 50} Y4 F10000                           # Go to starting point
  G0 Z0.4                                               # Raise Z to 0.4
  G91                                                   # Incremental positioning 
  G1 X100 E20 F1000                                     # Primeline
  G90         

[gcode_macro PRINT_END]
#   Use PRINT_END for the slicer ending script - please customise for your slicer of choice
gcode:
    {% set th = printer.toolhead %}
    {% set x_safe = th.position.x + 20 * (1 if th.axis_maximum.x - th.position.x > 20 else -1) %}
    {% set y_safe = th.position.y + 20 * (1 if th.axis_maximum.y - th.position.y > 20 else -1) %}
    {% set z_safe = [th.position.z + 2, th.axis_maximum.z]|min %}
    M220 S100 ; Set Feed rate to 100%
    M221 S100 ; Set Flow rate to 100%
    SET_PRESSURE_ADVANCE ADVANCE=0 ; Disable pressure advance
    G92 E0 ; zero the extruder
    G1 E-2.0 F3600 ; retract filament
    M400 ; wait for buffer to clear
    T0 ; Switch to tool 0
    G90 ; absolute positioning
    G0 X{x_safe} Y{y_safe} Z{z_safe} F20000 ; move nozzle to remove stringing
    G0 X{th.axis_maximum.x//2} Y{th.axis_maximum.y - 2} F3600 ; park nozzle at rear
    SET_GCODE_OFFSET X=0.0 Y=0.0 Z=0.0 ; reset gcode offset
    {% for tn in printer.toolchanger.tool_numbers %}
      M104 S0 T{tn} ; shutdown tool heater
      M107 T{tn} ; shutdown tool fan
      SET_STEPPER_ENABLE STEPPER=extruder{tn if tn > 0 else ''} ENABLE=0 ; disable extruder stepper
    {% endfor %}
  
    M140 S0;
  
    M117 Print done
    SET_SKEW CLEAR=1
    BED_MESH_CLEAR


[gcode_macro CLEAN_NOZZLE]
variable_start_x: 76 # starting position - offset a bit to the right from the brush
variable_start_y: -8 # starting Y position - offset a bit to the front of the brush
variable_start_z: 7   #starting Z height. This should barely touch the surface of the brush
variable_x_min: 40     #left side of the brush 
variable_x_max: 75     #right side of the brush
variable_y_min: -9 #front side of the brush - make sure the nozzle is over the last line of bristles 
variable_y_max: -6 #rear side of the brush - make sure the nozzle is over the first line of bristles
variable_z1: 6.8 # second z height of the nozzle over the brush - this should be with the nozzle slightly sunk in
variable_z2: 5.7 # third z height of the nozzle over the brush - this should be with the nozzle fully sunk into the brush, but not touching the bottom of it.
variable_wipe_qty: 2 #how many times to run the cleaning sequence
variable_wipe_spd: 200 #cleaning speed
variable_raise_distance: 10 # z height after cleaning is done
gcode:
  
  {% if "xyz" not in printer.toolhead.homed_axes %}
    G28
  {% endif %}
  
  G90  ; absolute positioning
  ### Move nozzle to start position
  G1 Z{start_z} F1500
  G1 X{start_x} Y{start_y} F9000
  G1 X{x_max} Y{y_min} F9000
  
  ### Wipe nozzle
  {% for wipes in range(1, (wipe_qty + 1)) %} #wipe left and right
    G1 Y{y_min} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    
    G1 Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    G1 Z{z1} F1500
    G1 Y{y_min} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    
    G1 Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    G1 Z{z2} F1500
    G1 Y{y_min} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    
    G1 Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} F{wipe_spd * 60}
    G1 X{start_x} F{wipe_spd * 60}
    
    G1 Z{start_z} F1500
  {% endfor %}
  
  {% for wipes in range(1, (wipe_qty + 1)) %} #wipe diagonally
    G1 Z{start_z} F1500
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
    G1 Z{z1} F1500
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
   G1 Z{z1} F1500
    G1 X{x_min} Y{y_max} F{wipe_spd * 60}
    G1 X{x_max} Y{y_min} F{wipe_spd * 60}
    G1 X{x_min} Y{y_max} F{wipe_spd * 60}
    G1 X{x_max} Y{y_min} F{wipe_spd * 60}
    G1 Z{z2} F1500
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} Y{y_min} F{wipe_spd * 60}
    G1 X{x_max} Y{y_max} F{wipe_spd * 60}
    G1 X{x_min} Y{y_max} F{wipe_spd * 60}
    G1 X{x_max} Y{y_min} F{wipe_spd * 60}
    G1 X{x_min} Y{y_max} F{wipe_spd * 60}
    G1 X{x_max} Y{y_min} F{wipe_spd * 60}
    G1 X{start_x} Y{y_min} F{wipe_spd * 60}
    
    G1 Z{start_z} F1500
  {% endfor %}
  
  G1 X{start_x} Y{start_y} F9000 #go back to starting position
  ## Raise nozzle
  G1 Z{raise_distance} 
  G1 Z10 F1500
  G1 X175 Y175 F9000
